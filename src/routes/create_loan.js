import CreateLoan from "../components/Loan/Create";

const create_loan = {
  name: "create_loan",
  exact: true,
  protected: true,
  path: "create-loan",
  component: <CreateLoan />,
};

export default create_loan;
